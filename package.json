{
  "name": "my-backend-api",
  "version": "1.0.0",
  "description": "A simple backend API",
  "main": "db.json",  
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
